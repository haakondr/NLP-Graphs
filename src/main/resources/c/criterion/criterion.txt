criterion criteria threshold cutoff requirements requirement definition thresholds cut-off inclusion aic assumption rules constraints eligibility constraint approach method cutoffs assumptions parameter